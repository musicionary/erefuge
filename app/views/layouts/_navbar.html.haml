.navbar-fixed
  %nav
    .nav-wrapper
      = link_to "Erefuge", root_path, {:class => "brand-logo black-text"}
      %a.button-collapse{"data-activates" => "mobile-demo", :href => "#"}
        %i.material-icons menu
      %ul.right.hide-on-med-and-down
        - if current_user
          %li= link_to "Your Account", account_path(current_user.account), {:class => "black-text"}
          %li= link_to "Events", events_path, {:class => "black-text"}
          %li= link_to "Log Out", destroy_user_session_path, {:class => "black-text nav-link", :method => :delete}
          - if current_user.admin?
            %li= link_to "Admin", rails_admin_path, {:class => "black-text"}
        - else
          %li.nav-link
            = link_to "Sign In", new_user_session_path, {:class => "black-text"}
          %li.nav-link
            = link_to "Sign Up", new_user_registration_path, {:class => "black-text"}
      %ul#mobile-demo.side-nav
        -# - if current_user
        -#   %li.nav-link
        -#     = link_to "Log Out", destroy_user_session_path, {:class => "black-text", :method => :delete}
        -# - if current_user.admin?
        -#       %li= link_to "Admin", rails_admin_path, {:class => "black-text"}
        -# - else
        -#   %li.nav-link
        -#     = link_to "Sign In", new_user_session_path, {:class => "black-text"}
        -#   %li.nav-link
        -#     = link_to "Sign Up", new_user_registration_path, {:class => "black-text"}
